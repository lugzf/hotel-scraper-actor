{
  "title": "Hotel Scraper Input",
  "description": "Lista de URLs dos hotéis para extração de dados",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "description": "URLs dos hotéis a serem processados",
      "type": "array",
      "editor": "requestListSources"
    }
  },
  "required": ["startUrls"]
}
